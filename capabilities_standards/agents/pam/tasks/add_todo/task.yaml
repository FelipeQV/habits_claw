name: add_todo
description: Create todo with active default status and optional priority/due date.
purpose: Create todo with active default status and optional priority/due date.
access_mode: write_append
