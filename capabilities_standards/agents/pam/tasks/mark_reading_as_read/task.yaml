name: mark_reading_as_read
description: Set reading status to read or unread and persist status transitions.
purpose: Set reading status to read or unread and persist status transitions.
access_mode: write_correction
