# update_cardio prompt

Ask for record_id or capture from context, confirm new minutes/type, execute correction SQL, describe delta and mark corrected entry.
User-visible response must start with `📝` as the first token.
