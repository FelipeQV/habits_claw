# report_workout prompt

Read the requested date/range, run SQL to aggregate cardio/routine minutes from non-corrected rows, explain totals plus derived `workout_done` and `cardio_total_min`.
User-visible response must start with `📖` as the first token.
