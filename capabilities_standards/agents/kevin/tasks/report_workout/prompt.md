# report_workout prompt

Read the requested date/range, run SQL to aggregate cardio/routine minutes from non-corrected rows, and if `exercise_name` is provided include last logged weight context for that exercise.
User-visible response must start with `📖` as the first token.
