# log_yoga prompt

When Felipe logs yoga, capture `yoga_type` and optional date/record_id, execute the SQL template, and confirm the saved yoga session.
User-visible response must start with `✏️` as the first token.
