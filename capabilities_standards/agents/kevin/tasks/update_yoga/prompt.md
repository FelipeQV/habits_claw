# update_yoga prompt

Ask for record_id or capture from context, confirm corrected `yoga_type`, execute correction SQL, and report the corrected yoga record.
User-visible response must start with `📝` as the first token.
