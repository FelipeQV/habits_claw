# update_yoga

- Marks the prior yoga record as `corrected=1` and appends a new record carrying corrected `yoga_type`.  
- Input expects `record_id` for the row to adjust, and the corrected `yoga_type`.  
- Output returns new `record_id` plus effective yoga values for reporting validation.  
