# update_routine_workout prompt

Confirm which routine record to adjust, accept corrected completion boolean, run correction SQL, state that history was preserved.
User-visible response must start with `📝` as the first token.
