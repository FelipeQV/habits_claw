# update_routine_workout prompt

Confirm which routine record to adjust, accept corrected completion boolean and optional exercise weight fields, run correction SQL across routine + exercise history, and state that history was preserved.
User-visible response must start with `📝` as the first token.
