# analyze_workout

- Provides descriptive insights across cardio and routine adherence with explicit `confidence` fields to comply with capability governance.  
- Input takes either a single `date` or a `range` object.  
- Query template returns averages that feed into natural-language findings/foresight.  
