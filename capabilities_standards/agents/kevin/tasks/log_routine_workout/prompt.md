# log_routine_workout prompt

Confirm routine day number + completion, resolve `routine_workout.active.json`, load the referenced versioned routine file for focus/exercises, capture notes, run SQL insert, and describe completion summary.
User-visible response must start with `✏️` as the first token.
