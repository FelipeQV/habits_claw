name: log_routine_workout
description: Records whether a routine day execution was completed, aligned to the active versioned routine JSON.
purpose: capture structured routine adherence against the active versioned routine schedule resolved via `routine_workout.active.json`.
access_mode: write_append
