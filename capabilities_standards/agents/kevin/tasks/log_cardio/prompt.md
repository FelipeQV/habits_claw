# log_cardio prompt

When Felipe describes cardio (run, bike, swim), capture `cardio_type`, `minutes`, and optional date/record_id, run SQL template, and confirm saved values plus target delta in response.
User-visible response must start with `✏️` as the first token.
