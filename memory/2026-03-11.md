
## 2026-03-11 13:55 CST — Durable memory flush
- Mindy capabilities migration direction was finalized around `capabilities_standards` terminology.
- Renamed workspace standards directory to `/home/ubuntu/.openclaw/workspace/capabilities_standards` and updated references accordingly.
- Created/normalized task `output.format.md` files for: `log_meal`, `update_meal`, `report_meal`, `analyze_meal`, `prescribe`, and `prescribe_delegation`.
- Corrected report rule: single-day selector applies only to delta snapshot (removed update-context wording).
- Added `output.format.md` as a required deliverable in governance `action_patterns`.
- Built and iterated the MINDY prompt-architecture split: created `/home/ubuntu/.openclaw/workspace-mindy/MINDY_ARCHITECTURE.md`, promoted draft to active `/home/ubuntu/.openclaw/workspace-mindy/MINDY_PROMPT.md`, and archived deprecated prompt at `/home/ubuntu/.openclaw/workspace-mindy/_archive/MINDY_PROMPT_DEPRECATED.md`.
- Established MECE split principle: prompt = conversational behavior/tone/decision rules; architecture = implementation/contracts/wiring.
- Added orchestration pattern for nutrition target updates in `capabilities_standards/governance/action_patterns.yaml`, including runtime verification item `runtime_query_must_hit_nutrition_targets_table`.
- DB target model changed multiple times, then was simplified: final active table is `nutrition_targets` in `/home/ubuntu/health_system.duckdb`; deprecated `nutrition_targets_versions` and `targets_versions` were dropped.
- `meals` target-link columns `target_version_id` and `nutrition_target_version_id` were created, then explicitly removed by user request.
- Added `agent_spec` target runtime fields: `runtime_source_of_truth = nutrition_targets` (DuckDB), `target_selection_rule` (`effective_to IS NULL` fallback latest `effective_from`), and `runtime_apply_scope = log, report, analyze`.
- Process lesson: Felipe requires a strict approval gate before mutations (propose exact diff first, wait for explicit GO, then execute); repeated violations occurred and were acknowledged.
- Decision logged (2026-03-11 19:25 CST): User approved creating an isolated factory scaffold at `/home/ubuntu/.openclaw/workspace/_factory/ghost_agent_template/` for a ghost prefilled template to automate capability creation from a future requirements form. Constraints: keep it as similar to Mindy as possible; include no more/no fewer files than needed; reuse existing templates where possible; use explicit placeholders for fixed values (e.g., DB path); keep it fully isolated from production to avoid mixing. Action deferred for now because user found a mistake to correct first.
